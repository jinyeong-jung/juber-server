type UpdateRideStatusResponse {
  ok: Boolean!
  error: String
  rideId: Int
}

enum statusOptions {
  ACCEPTED
  FINISHED
  CANCELED
  REQUESTING
  ONROUTE
}

type Mutation {
  UpdateRideStatus(
    rideId: Int!
    status: statusOptions!
  ): UpdateRideStatusResponse!
}
